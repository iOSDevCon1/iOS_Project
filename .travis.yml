language: objective-c
osx_image: xcode7
xcode_project: PodDrop.xcworkspace
xcode_scheme: PodDrop
xcode_sdk: iphonesimulator

script:
- pod install
- xcodebuild clean build test -project PodDrop.xcworkspace -scheme PodDrop -sdk iphonesimulator CODE_SIGNING_REQUIRED=NO

notifications:
  email: false
  slack: iosdevcon1:xEnP1AqmiZ3L44wbPQaNd2St
